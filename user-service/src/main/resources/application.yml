spring:
  application:
    name: user-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    kubernetes:
      enabled: false
      reload:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

